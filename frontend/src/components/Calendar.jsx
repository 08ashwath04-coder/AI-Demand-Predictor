import {useEffect,useState} from 'react'; import {getCalendarByMonth} from '../services/api';
const months=['January','February','March','April','May','June','July','August','September','October','November','December'];
export default function Calendar(){const [month,setMonth]=useState(new Date().getMonth()+1);const [events,setEvents]=useState([]);const [loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);getCalendarByMonth(month).then(setEvents).finally(()=>setLoading(false));},[month]); return <div className="panel"><div className="panel-head"><div><div className="eyebrow">365-DAY GEO-CULTURAL CALENDAR</div><h2>Smart calendar</h2></div><select value={month} onChange={e=>setMonth(+e.target.value)}>{months.map((m,i)=><option key={m} value={i+1}>{m}</option>)}</select></div>{loading?<div className="loading">Loading events…</div>:<div className="events">{events.map(e=><div className="event" key={e.id}><div className="event-date">{new Date(e.date).toLocaleDateString('en-IN',{day:'2-digit',month:'short'})}</div><div><b>{e.title}</b><p>{e.description}</p></div><span className="chip">{e.event_type}</span></div>)}{!events.length&&<div className="empty">No events for this month.</div>}</div>}</div>}
