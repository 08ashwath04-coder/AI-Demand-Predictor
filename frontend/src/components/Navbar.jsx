import { useNavigate } from 'react-router-dom';
export default function Navbar(){const nav=useNavigate(); const user=JSON.parse(localStorage.getItem('user')||'null'); const logout=()=>{localStorage.clear();nav('/login')}; return <header className="navbar"><div className="brand" onClick={()=>nav('/')}><div className="brand-mark">DB</div><div><b>DemandBridge</b><span>AI</span></div></div><div className="nav-actions">{user?<><button className="ghost" onClick={()=>nav(user.role==='farmer'?'/farmer':'/retailer')}>Dashboard</button><button className="ghost" onClick={logout}>Logout</button></>:<><button className="ghost" onClick={()=>nav('/login')}>Login</button><button className="primary" onClick={()=>nav('/register')}>Get Started</button></>}</div></header>}
