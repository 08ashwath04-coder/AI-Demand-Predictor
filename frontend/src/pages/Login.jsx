import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {loginUser} from '../services/api';
export default function Login(){const nav=useNavigate(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const submit=async e=>{e.preventDefault();setError('');try{const r=await loginUser({email,password});localStorage.setItem('token',r.access_token);localStorage.setItem('user',JSON.stringify(r.user));nav(r.user.role==='farmer'?'/farmer':'/retailer')}catch(err){setError(err.response?.data?.detail||'Login failed')}};return <><NavbarAuth/><div className="auth-page"><form className="auth-card" onSubmit={submit}><div className="eyebrow">DEMANDBRIDGE AI</div><h1>Welcome back</h1><p>Sign in to your operational command center.</p>{error&&<div className="error">{error}</div>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary big full">Login</button><p className="auth-foot">No account? <button type="button" className="link" onClick={()=>nav('/register')}>Register</button></p></form></div></>}
function NavbarAuth(){return <header className="navbar"><div className="brand" onClick={()=>location.href='/'}><div className="brand-mark">DB</div><b>DemandBridge <span>AI</span></b></div></header>}
